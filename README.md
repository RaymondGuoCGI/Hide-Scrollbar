# Hide Scrollbar

A minimal Chrome extension that injects CSS to hide scrollbars on all pages while keeping scroll behavior intact.

## Install (unpacked)
1. Open `chrome://extensions`.
2. Enable "Developer mode".
3. Click "Load unpacked" and select this folder.

## Notes
- Chrome internal pages like `chrome://extensions` cannot be styled by extensions due to browser restrictions.
